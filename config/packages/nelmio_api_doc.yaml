nelmio_api_doc:
    documentation:
        info:
            title: User Service
            description: 用户模块基础服务
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/
